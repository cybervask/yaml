// Package yaml provides a high-performance, drop-in replacement for [go.yaml.in/yaml/v4]
// that extends standard YAML decoding workflows with native support for recursive structural
// default values, environment variable injections, and semantic tag-based model validation.
//
// All standard encoding, decoding, streaming primitives, and functional configuration choices
// are fully re-exported to maintain seamless backward compatibility with existing projects.
package yaml
